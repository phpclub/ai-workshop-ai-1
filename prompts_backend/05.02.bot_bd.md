Напиши модуль database

Используй asyncpg для async соединения с PostgreSQL.
Нужны только функции для первого этапа (без RAG):
- init_db() — создаёт таблицы applications и knowledge_chunks
- save_application(name, phone, question, user_id) — сохраняет заявку

Схема из архитектуры: [вставляешь схему БД из шага 2]

Добавь docstring к каждой функции на русском.
Обработай возможные ошибки подключения.
