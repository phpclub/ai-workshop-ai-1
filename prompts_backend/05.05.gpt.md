Обнови bot — замени заглушку на реальные ответы через GPT.

Новый handler для текстовых сообщений:
1. Вызвать rag.search(user_message) → получить релевантные чанки
2. Собрать промпт: системный промпт + найденные чанки + вопрос
3. Отправить в OpenAI GPT-4o-mini
4. Вернуть ответ пользователю

Если RAG ничего не нашёл (пустой результат) — 
бот отвечает что не знает и предлагает записаться.

Не трогай FSM для записи — он уже работает.
